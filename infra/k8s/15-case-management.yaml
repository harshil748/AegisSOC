apiVersion: apps/v1
kind: Deployment
metadata:
  name: aegis-case-management
  namespace: aegissoc
  labels:
    app: aegis-case-management
    app.kubernetes.io/part-of: aegissoc
    app.kubernetes.io/component: case_management
spec:
  replicas: 2
  selector:
    matchLabels:
      app: aegis-case-management
  template:
    metadata:
      labels:
        app: aegis-case-management
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8006"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: case-management
          image: ghcr.io/aegissoc/case_management:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8006
              name: http
          envFrom:
            - configMapRef:
                name: aegis-config
            - secretRef:
                name: aegis-secrets
          env:
            - name: SERVICE_PORT
              value: "8006"
          readinessProbe:
            httpGet:
              path: /ready
              port: 8006
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: "100m"
              memory: "192Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
      # TODO: add topologySpreadConstraints / podAntiAffinity across zones at scale.
---
apiVersion: v1
kind: Service
metadata:
  name: aegis-case-management
  namespace: aegissoc
  labels:
    app: aegis-case-management
spec:
  selector:
    app: aegis-case-management
  ports:
    - name: http
      port: 8006
      targetPort: 8006
  type: ClusterIP
