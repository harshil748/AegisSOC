apiVersion: apps/v1
kind: Deployment
metadata:
  name: aegis-ingestion
  namespace: aegissoc
  labels:
    app: aegis-ingestion
    app.kubernetes.io/part-of: aegissoc
    app.kubernetes.io/component: ingestion
spec:
  replicas: 2
  selector:
    matchLabels:
      app: aegis-ingestion
  template:
    metadata:
      labels:
        app: aegis-ingestion
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8001"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: ingestion
          image: ghcr.io/aegissoc/ingestion:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8001
              name: http
          envFrom:
            - configMapRef:
                name: aegis-config
            - secretRef:
                name: aegis-secrets
          env:
            - name: SERVICE_PORT
              value: "8001"
          readinessProbe:
            httpGet:
              path: /ready
              port: 8001
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8001
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: "100m"
              memory: "192Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
      # TODO: add topologySpreadConstraints / podAntiAffinity across zones at scale.
---
apiVersion: v1
kind: Service
metadata:
  name: aegis-ingestion
  namespace: aegissoc
  labels:
    app: aegis-ingestion
spec:
  selector:
    app: aegis-ingestion
  ports:
    - name: http
      port: 8001
      targetPort: 8001
  type: ClusterIP
