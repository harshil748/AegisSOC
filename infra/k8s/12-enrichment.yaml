apiVersion: apps/v1
kind: Deployment
metadata:
  name: aegis-enrichment
  namespace: aegissoc
  labels:
    app: aegis-enrichment
    app.kubernetes.io/part-of: aegissoc
    app.kubernetes.io/component: enrichment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: aegis-enrichment
  template:
    metadata:
      labels:
        app: aegis-enrichment
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8003"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: enrichment
          image: ghcr.io/aegissoc/enrichment:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8003
              name: http
          envFrom:
            - configMapRef:
                name: aegis-config
            - secretRef:
                name: aegis-secrets
          env:
            - name: SERVICE_PORT
              value: "8003"
          readinessProbe:
            httpGet:
              path: /ready
              port: 8003
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8003
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: "100m"
              memory: "192Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
      # TODO: add topologySpreadConstraints / podAntiAffinity across zones at scale.
---
apiVersion: v1
kind: Service
metadata:
  name: aegis-enrichment
  namespace: aegissoc
  labels:
    app: aegis-enrichment
spec:
  selector:
    app: aegis-enrichment
  ports:
    - name: http
      port: 8003
      targetPort: 8003
  type: ClusterIP
